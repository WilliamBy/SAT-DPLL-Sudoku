verify程序为验证程序，验证计算结果是否正确，使用方法为：cmd—verify.exe  *.cnf  *.res；（使用绝对路径或者相对路径，相对路径需要三个文件在同一文件夹下）
      //注意：有些同学生成的解文件不符合格式要求，也没法用此验证程序检测.当算例变元数较大，如超过1000时，可用verify5000.exe进行检验

另外，输出文件请严格按照指导书中的格式，否则verify程序无法正常工作。